<h1>Tasks List</h1>

<% @tasks.each do |t| %>
  <ul>
    <li><%= link_to t.name, task_path(t) %></li>
  </ul>
<% end %>

<%= link_to "New task", new_task_path, class: 'pure-button pure-button-primary' %>
<%= link_to "Go to home page", root_path, class: 'pure-button pure-button-secondary' %>